services:
  - type: web
    name: guitar-practice-api
    runtime: docker
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: CORS_ORIGINS
        sync: false
